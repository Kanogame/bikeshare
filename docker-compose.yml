services:
  postgres:
    image: postgres:latest
    container_name: bikeshare_pg
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: zeRdojD7
      POSTGRES_DB: orag_dev
    ports:
      - 5432:5432
    volumes:
      - bikeshare_pg_vol:/var/lib/postgresql
    restart: always

volumes:
 bikeshare_pg_vol:
